<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Кеш списка сущностей для разметки (module + world)
    |--------------------------------------------------------------------------
    */

    'entities_ttl_seconds' => (int) env('MARKUP_ENTITIES_TTL', 600),

    /*
    |--------------------------------------------------------------------------
    | Кеш превью одной сущности (world + module + entity)
    |--------------------------------------------------------------------------
    */

    'preview_ttl_seconds' => (int) env('MARKUP_PREVIEW_TTL', 300),

    /*
    |--------------------------------------------------------------------------
    | Кеш целого батча resolve по нормализованному набору refs
    |--------------------------------------------------------------------------
    */

    'resolve_batch_ttl_seconds' => (int) env('MARKUP_RESOLVE_BATCH_TTL', 45),

    /*
    |--------------------------------------------------------------------------
    | Лимит запросов resolve на пользователя в минуту (RateLimiter)
    |--------------------------------------------------------------------------
    */

    'resolve_rate_limit_per_minute' => (int) env('MARKUP_RESOLVE_RATE_LIMIT', 120),

];
